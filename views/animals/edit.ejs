<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Animal</title>
</head>
<body>
    <h1>Edit Animal</h1>
    <form action="/animals/<%= animal.id %> ?_method=PUT"
     method="POST" autocomplete="off">
        <input type="text" name="animal" value="<%= animal.animal %>">
        <input name="fur" type="checkbox" <%= animal.fur ? 'checked' : '' %>>
        <button type="submit">Save Animal</button>
    </body>
</html>