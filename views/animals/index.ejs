<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animals in Express</title>
</head>
<body>
    <h1>Animals</h1>
    <ul>
        <% animals.forEach(function(a) { %>
            <li>
                <form action="/animals/<%= a.id %>?_method=DELETE" class="delete-form" method="POST">
                <button type="submit">X</button>
                  </form>
                  <a href="/animals/<%= a.id %>"><%= a.animal %></a>
                <%= a.animal %>
                <br>
                <%= a.fur ? "Brush me!" : "Don't brush me!" %>
            </li>
        <% }) %>
    </ul>
    <a href="/animals/new">Add Animal</a>
    
</body>
</html>